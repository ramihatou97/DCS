#!/usr/bin/env bash
# shell: bash
# DCS Backend Environment Configuration

# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# LLM API Keys (at least one required)
# Get your keys from:
# - Anthropic: https://console.anthropic.com/
# - OpenAI: https://platform.openai.com/
# - Google: https://makersuite.google.com/
#
# ⚠️ SECURITY WARNING:
# - NEVER commit real API keys to git
# - Copy this file to .env and add your real keys there
# - The .env file is already in .gitignore for your protection

ANTHROPIC_API_KEY=your_anthropic_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
GOOGLE_API_KEY=your_google_api_key_here

# Default LLM Model (optional)
DEFAULT_LLM_MODEL=claude-sonnet-3.5

# Cache Configuration
CACHE_TTL=3600                  # Cache time-to-live in seconds (default: 3600 = 1 hour)
CACHE_MAX_SIZE=100              # Maximum number of cached entries (default: 100)

# Rate Limiting Configuration
API_RATE_WINDOW=900000          # API rate limit window in milliseconds (default: 900000 = 15 minutes)
API_RATE_MAX=100                # Maximum API requests per window (default: 100)
LLM_RATE_WINDOW=3600000         # LLM rate limit window in milliseconds (default: 3600000 = 1 hour)
LLM_RATE_MAX=50                 # Maximum LLM requests per window (default: 50)

# Quality Metrics Configuration (optional)
QUALITY_THRESHOLD=0.8           # Minimum quality score for acceptance (default: 0.8 = 80%)

# Database Configuration (optional - for future use)
# DATABASE_URL=postgresql://user:password@localhost:5432/dcs

