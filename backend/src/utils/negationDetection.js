/**
 * Negation Detection - Stub for backend
 * TODO: Copy full implementation from frontend
 */

const validateComplicationExtraction = (complication, context) => {
  // Stub implementation - accepts all for now
  return {
    valid: true,
    negated: false,
    confidence: 0.5
  };
};

module.exports = {
  validateComplicationExtraction
};
